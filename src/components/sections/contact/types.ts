import { LucideIcon } from 'lucide-react';

export interface HiringModel {
  value: string;
  label: string;
}

export interface ContactInfoItem {
  icon: LucideIcon;
  label: string;
  value: string;
}